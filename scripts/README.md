# Scripts Directory

This is the repository-local script entry point for **jepa-learning-order**.

If you are working inside this folder, assume the repository root is one level up. Prefer these local scripts over the shared handbook scripts when running project-specific submissions or retrievals.
